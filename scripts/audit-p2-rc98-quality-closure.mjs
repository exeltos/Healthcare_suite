import fs from'node:fs';const r=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');const i=r('src/pages/Quality/IncidentsPage.jsx'),c=r('src/pages/Quality/CapaPage.jsx'),k=r('src/pages/Quality/RisksPage.jsx');const x=[['incident blocks active CAPA',i.includes('activeCapa.length')],['incident blocks ineffective CAPA',i.includes('ineffectiveCapa.length')],['incident owner',i.includes('Assign an investigation owner')],['severe root cause',i.includes('requires an investigation conclusion')],['severe lessons',i.includes('recurrence-prevention actions')],['incident marker',i.includes('closureVerified:true')],['CAPA effectiveness gate',c.includes("record.status==='Ολοκληρωμένη'&&record.effectivenessStatus!=='Αποτελεσματική'")],['CAPA marker',c.includes('closureVerified:true')],['risk owner',k.includes('Assign a risk owner')],['risk controls',k.includes('Document implemented controls')],['residual rationale',k.includes('High residual risk requires documented')],['risk marker',k.includes('closedOrAcceptedAt:new Date().toISOString(),closureVerified:true')]];let f=0;for(const[n,o]of x){console.log(`${o?'PASS':'FAIL'} ${n}`);if(!o)f++}console.log(`P2 rc.98 quality closure audit: ${x.length-f}/${x.length}`);if(f)process.exit(1)