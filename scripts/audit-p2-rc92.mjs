import fs from'node:fs';const s=fs.readFileSync(new URL('../src/pages/Lira/LiraPage.jsx',import.meta.url),'utf8');const c=[['bilingual signal rules',s.includes('function signalCopy')&&s.includes('Overdue CAPA')],['evidence basis',s.includes('copy.basis')&&s.includes('Basis:')],['source links',s.includes('Open source')],['data quality',s.includes('dataQuality.denominatorReady')],['missing indicators',s.includes('indicatorMissing')]];let f=0;for(const[n,o]of c){console.log(`${o?'PASS':'FAIL'} ${n}`);if(!o)f++}console.log(`P2 rc.92: ${c.length-f}/${c.length}`);if(f)process.exit(1)